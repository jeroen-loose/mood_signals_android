<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/HomeFragment">

    <fragment
        android:id="@+id/HomeFragment"
        android:name="com.loosethread.moodsignals.FragmentHome"
        android:label="@string/home_fragment_label"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_HomeFragment_to_SignalsFragment"
            app:destination="@id/SignalsFragment" />
    </fragment>
    <fragment
        android:id="@+id/SignalsFragment"
        android:name="com.loosethread.moodsignals.FragmentSignals"
        android:label="@string/signals_fragment_label"
        tools:layout="@layout/fragment_signals">

        <action
            android:id="@+id/action_SignalsFragment_to_HomeFragment"
            app:destination="@id/HomeFragment" />
    </fragment>
</navigation>